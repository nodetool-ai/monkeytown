# Monkeytown Environment Configuration
# ======================================
# Copy this file to .env and fill in the values
# NEVER commit .env with real values to version control

# -----------------------------------------------------------------------------
# Core Application Settings
# -----------------------------------------------------------------------------
NODE_ENV=development

# -----------------------------------------------------------------------------
# Web Application (Frontend)
# -----------------------------------------------------------------------------
WEB_PORT=3000

# API endpoint for HTTP requests
VITE_API_URL=http://localhost:8080

# WebSocket endpoint for real-time communication
VITE_WS_URL=ws://localhost:8080

# -----------------------------------------------------------------------------
# Event Stream Server
# -----------------------------------------------------------------------------
EVENT_STREAM_PORT=8080

# Redis connection string (for event persistence in production)
REDIS_URL=redis://localhost:6379

# GitHub token for agent workflow operations
# Generate at: https://github.com/settings/tokens
# Required scopes: repo, workflow, read:org
GITHUB_TOKEN=

# MiniMax API key for agent cognition
# Get from: https://api.minimax.chat
MINIMAX_API_KEY=

# -----------------------------------------------------------------------------
# AI Game Opponents (MiniMax via Anthropic-compatible API)
# -----------------------------------------------------------------------------
# Uses MiniMax API with Anthropic-compatible endpoint for LLM game AI opponents
# The existing MINIMAX_API_KEY is used for authentication

# Base URL for Anthropic-compatible API (MiniMax provides Anthropic compatibility)
# Default: https://api.minimax.io/anthropic
ANTHROPIC_BASE_URL=https://api.minimax.io/anthropic

# Model to use for AI game opponents
# Default: MiniMax-M2
# Options: MiniMax-M2, MiniMax-M2.1
ANTHROPIC_MODEL=MiniMax-M2

# -----------------------------------------------------------------------------
# Development Tools
# -----------------------------------------------------------------------------
# Enable debug logging (comma-separated namespaces)
DEBUG=monkeytown:*

# -----------------------------------------------------------------------------
# External Services
# -----------------------------------------------------------------------------
# Vercel deployment token (for CI/CD)
# Generate at: https://vercel.com/settings/tokens
VERCEL_TOKEN=

# GitHub repository for workflows
REPO_OWNER=anomalyco
REPO_NAME=monkeytown

# -----------------------------------------------------------------------------
# Optional Features
# -----------------------------------------------------------------------------
# Enable metrics collection
METRICS_ENABLED=true

# Session secret for development (use random string in production)
SESSION_SECRET=dev-secret-change-in-production

# CORS origins (comma-separated for multiple)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# -----------------------------------------------------------------------------
# Performance Tuning
# -----------------------------------------------------------------------------
# Maximum WebSocket connections per instance
WS_MAX_CONNECTIONS=1000

# Event buffer size (number of events to buffer)
EVENT_BUFFER_SIZE=10000

# Connection heartbeat interval (ms)
WS_HEARTBEAT_INTERVAL=30000

# -----------------------------------------------------------------------------
# Security (Development)
# -----------------------------------------------------------------------------
# Set to "true" in production
ENABLE_RATE_LIMITING=false

# Rate limit window (seconds)
RATE_LIMIT_WINDOW=60

# Rate limit max requests per window
RATE_LIMIT_MAX=100

# -----------------------------------------------------------------------------
# Monitoring (Optional)
# -----------------------------------------------------------------------------
# Sentry DSN for error tracking (optional)
# SENTRY_DSN=

# Datadog API key for metrics (optional)
# DATADOG_API_KEY=
