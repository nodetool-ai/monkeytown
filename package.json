{
  "name": "monkeytown",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix server",
    "build": "npm run build --prefix server",
    "start": "npm run start --prefix server"
  },
  "workspaces": [
    "server",
    "packages/*"
  ]
}
